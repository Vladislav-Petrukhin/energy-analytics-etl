# 📊 Резюме проекта и результаты выполнения

## 🎯 Что мы делали

### Основная задача
Адаптировали ETL (Extract, Transform, Load) пайплайн для обработки реального датасета энергопотребления с нестандартной структурой и множественными форматами данных.

### Проблемы, которые решали

#### 1. **Структура данных**
- **Проблема**: Датасет состоял из 336 CSV файлов, разбросанных по 3 вложенным папкам
  - `daily_dataset/daily_dataset/` - 112 файлов
  - `halfhourly_dataset/halfhourly_dataset/` - 112 файлов
  - `hhblock_dataset/hhblock_dataset/` - 112 файлов
- **Решение**: 
  - Модифицировали `extract.py` для рекурсивного поиска CSV файлов (`**/*.csv`)
  - Добавили группировку файлов по папкам для определения формата

#### 2. **Множественные форматы данных**
- **Проблема**: Файлы в разных папках имели совершенно разные структуры:
  
  **Формат 1** (daily_dataset):
  ```
  id, day, energy_median, energy_mean, energy_max, energy_count, energy_std, energy_sum, energy_min
  ```
  
  **Формат 2** (halfhourly_dataset):
  ```
  id, tstp, energy(kWh/hh)
  ```
  
  **Формат 3** (hhblock_dataset):
  ```
  LCLid, day, hh_0, hh_1, hh_2, ..., hh_47
  ```
  
  (48 колонок с почасовыми данными)

- **Решение**: 
  - Создали автоматическое определение формата по колонкам
  - Реализовали 3 функции преобразования:
    - `process_aggregated_format()` - для агрегированных данных
    - `process_kaggle_format()` - для формата Kaggle с временными метками
    - `process_hourly_columns_format()` - для развертывания почасовых колонок в строки
  - Все форматы преобразуются в единую схему: `LCLid, day, energy_kwh, timestamp`

#### 3. **Архитектура и доступ**
- **Проблема**: Airflow контейнер не мог выполнять `docker exec` внутри Docker окружения
- **Решение**: 
  - Перешли с `BashOperator` на `PythonOperator`
  - PySpark теперь запускается напрямую из контейнера Airflow
  - Обновили Dockerfile для установки PySpark и Java

#### 4. **Оптимизация памяти**
- **Проблема**: Обработка 336 файлов требовала слишком много памяти
- **Решение**:
  - Убрали вызовы `.count()` перед загрузкой (требуют материализации всего DataFrame)
  - Заменили `collect()` на `toLocalIterator()` для обработки батчами
  - Добавили батчевую загрузку в PostgreSQL (по 5000 записей)

#### 5. **Загрузка в PostgreSQL**
- **Проблема**: Неправильный формат SQL запроса для `execute_values`
- **Решение**: Использовали правильный формат `VALUES %s` вместо `VALUES (%s, %s, ...)`

---

## 🏗️ Архитектура решения

### ETL Пайплайн (3 этапа)

#### **1. Extract (Извлечение)**
- **Что делает**: 
  - Рекурсивно находит все CSV файлы в `data/raw/`
  - Группирует их по папкам
  - Определяет формат каждого набора файлов
  - Преобразует данные в единую схему
  - Объединяет все датасеты
  - Очищает дубликаты
  - Загружает в `raw.energy_consumption` (PostgreSQL)

- **Результат**: 
  - ~2.9 миллиона записей в сырой таблице
  - Унифицированный формат: `LCLid, day, energy_kwh, timestamp`

#### **2. Transform (Трансформация)**
- **Что делает**:
  - Читает данные из `raw.energy_consumption`
  - Выявляет аномалии (запредельные значения)
  - Агрегирует данные по разным уровням:
    - **По часам**: среднее потребление по часам дня
    - **По дням**: дневная статистика
    - **По неделям**: недельная статистика
    - **По месяцам**: месячная статистика
    - **По домохозяйствам**: индивидуальная статистика для каждого LCLid
  - Сохраняет результаты в Parquet файлы в `data/processed/`

- **Результат**:
  - 6 наборов агрегированных данных:
    - `hourly` - почасовая статистика
    - `daily` - дневная статистика
    - `weekly` - недельная статистика
    - `monthly` - месячная статистика
    - `household_stats` - статистика по домохозяйствам
    - `anomalies` - обнаруженные аномалии (~59,000 записей)

#### **3. Load (Загрузка в DWH)**
- **Что делает**:
  - Читает Parquet файлы из `data/processed/`
  - Загружает данные в аналитические таблицы PostgreSQL:
    - `analytics.hourly_consumption`
    - `analytics.daily_consumption`
    - `analytics.weekly_consumption`
    - `analytics.monthly_consumption`
    - `analytics.household_stats`
    - `analytics.anomalies`
  - Создает индексы для оптимизации запросов

- **Результат**:
  - Готовый Data Warehouse с аналитическими таблицами
  - Оптимизированные индексы для быстрых запросов

---

## 📈 Полученные результаты

### Данные в базе

#### Сырые данные (raw schema)
- **Таблица**: `raw.energy_consumption`
- **Записей**: ~2,897,704
- **Формат**: 
  - `lclid` - идентификатор домохозяйства
  - `day` - дата
  - `energy_kwh` - потребление энергии в кВт·ч
  - `timestamp` - временная метка

#### Аналитические данные (analytics schema)

1. **`analytics.hourly_consumption`**
   - Агрегированные данные по часам дня
   - Использование: анализ паттернов потребления по времени суток

2. **`analytics.daily_consumption`**
   - Дневная статистика потребления
   - Использование: анализ трендов по дням

3. **`analytics.weekly_consumption`**
   - Недельная статистика
   - Использование: анализ недельных циклов

4. **`analytics.monthly_consumption`**
   - Месячная статистика
   - Использование: анализ сезонных трендов

5. **`analytics.household_stats`**
   - Индивидуальная статистика по каждому домохозяйству
   - Использование: персонализированная аналитика

6. **`analytics.anomalies`**
   - Обнаруженные аномалии в потреблении (~59,763 записей)
   - Использование: выявление проблем, мониторинг

---

## 🔧 Технологический стек

- **Orchestration**: Apache Airflow 2.8.0
- **Data Processing**: Apache Spark 3.5.0 (PySpark)
- **Database**: PostgreSQL 15
- **Containerization**: Docker & Docker Compose
- **Languages**: Python 3.8

---

## 💡 Зачем это нужно

### Бизнес-ценность

1. **Унификация данных**
   - Разные источники данных (3 формата) приведены к единому стандарту
   - Упрощает дальнейшую работу с данными

2. **Аналитика**
   - Готовая структура для построения отчетов и дашбордов
   - Разные уровни агрегации для разных типов анализа

3. **Выявление аномалий**
   - Автоматическое обнаружение необычных паттернов потребления
   - Возможность раннего выявления проблем

4. **Масштабируемость**
   - ETL пайплайн может обрабатывать большие объемы данных
   - Автоматизированный процесс не требует ручного вмешательства

### Техническая ценность

1. **Автоматизация**
   - Весь процесс выполняется автоматически по расписанию
   - Можно добавить новые данные и они будут обработаны автоматически

2. **Надежность**
   - Обработка ошибок на каждом этапе
   - Возможность повторного запуска при сбоях

3. **Гибкость**
   - Легко добавить новые форматы данных
   - Можно расширить трансформации

---

## 📊 Примеры использования данных

### 1. Анализ потребления по часам
```sql
SELECT hour, AVG(energy_kwh) as avg_consumption
FROM analytics.hourly_consumption
GROUP BY hour
ORDER BY hour;
```
**Результат**: График среднего потребления по часам дня

### 2. Топ домохозяйств по потреблению
```sql
SELECT lclid, total_consumption, avg_daily_consumption
FROM analytics.household_stats
ORDER BY total_consumption DESC
LIMIT 10;
```
**Результат**: Список домохозяйств с наибольшим потреблением

### 3. Выявление аномалий
```sql
SELECT lclid, timestamp, energy_kwh, anomaly_type
FROM analytics.anomalies
WHERE day = CURRENT_DATE - INTERVAL '7 days';
```
**Результат**: Аномалии за последнюю неделю

### 4. Сезонный анализ
```sql
SELECT month, AVG(energy_kwh) as avg_monthly_consumption
FROM analytics.monthly_consumption
GROUP BY month
ORDER BY month;
```
**Результат**: Сезонные тренды потребления

---

## 🎓 Что мы научились

1. **Работа с разнородными данными**
   - Автоматическое определение форматов
   - Преобразование разных структур в единую схему

2. **Оптимизация памяти**
   - Эффективная обработка больших объемов данных
   - Батчевая загрузка вместо полной материализации

3. **Интеграция компонентов**
   - Airflow + Spark + PostgreSQL
   - Правильная настройка Docker для работы всех компонентов

4. **Обработка ошибок**
   - Понимание проблем сериализации в Spark
   - Правильное использование библиотек (psycopg2.execute_values)

---

## 🚀 Дальнейшее развитие

### Возможные улучшения:

1. **Добавить больше форматов данных**
   - Если появятся новые источники, легко добавить новые функции обработки

2. **Улучшить детекцию аномалий**
   - Более сложные алгоритмы (машинное обучение)
   - Автоматические алерты

3. **Добавить визуализацию**
   - Streamlit дашборд для просмотра данных
   - Интерактивные графики и отчеты

4. **Оптимизация производительности**
   - Партиционирование данных
   - Кэширование промежуточных результатов

---

## ✅ Итог

**Мы успешно создали полноценный ETL пайплайн, который:**

✅ Обрабатывает данные из 3 разных форматов  
✅ Преобразует их в единую структуру  
✅ Выявляет аномалии  
✅ Создает аналитические таблицы  
✅ Работает автоматически по расписанию  
✅ Готов для дальнейшей аналитики и визуализации  

**Проект готов к использованию!** 🎉

