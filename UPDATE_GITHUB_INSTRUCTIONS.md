# 📤 Инструкция по обновлению проекта на GitHub

## Способ 1: Автоматический (рекомендуется)

Запустите файл `update_github.bat` (двойной клик)

---

## Способ 2: Ручной (если автоматический не работает)

Выполните команды в командной строке (cmd, не PowerShell):

### Шаг 1: Проверка статуса

```bash
git status
```

Проверьте, какие файлы изменены или удалены.

### Шаг 2: Добавление всех изменений

```bash
git add .
```

Это добавит все изменения (включая удаленные файлы) в staging area.

### Шаг 3: Создание коммита

```bash
git commit -m "Clean up project: remove temporary files and update documentation"
```

Или используйте свое сообщение коммита, например:
```bash
git commit -m "Удалены временные файлы, обновлена документация"
```

### Шаг 4: Загрузка на GitHub

```bash
git push origin main
```

Если используется ветка `master` вместо `main`:
```bash
git push origin master
```

---

## Что будет загружено:

✅ Все изменения в документации
✅ Удаление временных файлов (git отследит удаления)
✅ Обновленный код (если были изменения)
✅ Новые файлы (если были добавлены)

---

## После выполнения:

1. Откройте ваш репозиторий на GitHub
2. Проверьте, что все изменения загружены
3. Убедитесь, что удаленные файлы больше не отображаются

---

**Готово! Проект обновлен на GitHub! 🎉**

